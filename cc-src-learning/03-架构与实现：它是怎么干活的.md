# 第三章：架构与实现——它是怎么"干活"的？

如果说前两章让我们理解了 Claude Code 的设计理念和核心模块，那么这一章我们将深入到它的"引擎盖下"，看看这些设计是如何在代码中实现的。

这一章，我们将依次分析：
- **查询引擎**：Claude Code 是如何调用 AI 的
- **桥接系统**：IDE 与 CLI 之间的通信机制
- **服务层**：API 调用、MCP、OAuth 等外部服务如何集成
- **启动优化**：如何让一个 51 万行代码的项目秒级启动
- **条件编译**：Bun 的 `feature()` 如何实现"一码多构"

让我们开始吧。

---

## 3.1 查询引擎（QueryEngine）：AI 是怎么调用的？

### QueryEngine 是什么？

在 Claude Code 中，所有与 AI 的对话都通过 `QueryEngine` 来处理。你可以把它想象成整个系统的心脏——每一次 AI 回复、每一个工具调用，都经过这里。

```typescript
// src/QueryEngine.ts
export class QueryEngine {
  private config: QueryEngineConfig
  private mutableMessages: Message[]
  private abortController: AbortController
  private permissionDenials: SDKPermissionDenial[]
  private totalUsage: NonNullableUsage
  // ...
}
```

### 核心配置

创建 QueryEngine 时需要传入一堆配置（`QueryEngineConfig`），这就像组装一台电脑——不同的配置会得到不同能力的 AI：

```typescript
export type QueryEngineConfig = {
  cwd: string                        // 当前工作目录
  tools: Tools                      // 可用的工具列表
  commands: Command[]                // 可用的命令列表
  mcpClients: MCPServerConnection[]  // MCP 服务器连接
  agents: AgentDefinition[]          // Agent 定义
  canUseTool: CanUseToolFn          // 权限检查函数
  getAppState: () => AppState       // 应用状态获取函数
  // ...
  customSystemPrompt?: string        // 自定义系统提示词
  userSpecifiedModel?: string       // 指定使用的模型
  thinkingConfig?: ThinkingConfig    // 思考模式配置
  maxTurns?: number                // 最大对话轮次
  maxBudgetUsd?: number             // 最大花费限制
}
```

### 主循环：消息处理

QueryEngine 的核心是一个"消息处理循环"：

```typescript
// 简化版流程
async function submitMessage(userMessage: Message): Promise<void> {
  // 1. 将用户消息添加到历史
  this.mutableMessages.push(userMessage)

  // 2. 构建 AI 请求
  const apiRequest = await this.buildAPIRequest()

  // 3. 发送给 AI
  const response = await this.callAI(apiRequest)

  // 4. 处理 AI 的响应
  for (const block of response.content) {
    if (block.type === 'text') {
      // 处理文本回复
      this.handleTextBlock(block)
    } else if (block.type === 'tool_use') {
      // 处理工具调用
      await this.handleToolCall(block)
    }
  }

  // 5. 将 AI 回复添加到历史
  this.mutableMessages.push(aiResponse)
}
```

### 工具调用的处理

当 AI 说"我要调用工具"时，QueryEngine 会：

```typescript
// 处理工具调用的简化流程
async function handleToolCall(toolUse: ToolUseBlock) {
  // 1. 检查权限
  const permission = await this.checkPermission(toolUse.name)

  if (!permission.allowed) {
    // 被拒绝，返回错误给 AI
    return this.createToolError(toolUse.id, permission.reason)
  }

  // 2. 执行工具
  const result = await this.executeTool(toolUse.name, toolUse.input)

  // 3. 返回结果给 AI
  return this.createToolResult(toolUse.id, result)
}
```

### 重试与错误处理

AI 调用不是 100% 可靠的——网络会抖动，API 会限流。QueryEngine 有一套完善的错误处理机制：

```typescript
// 错误分类处理
const categorizeRetryableAPIError = (error: Error) => {
  if (error instanceof RateLimitError) {
    return 'rate_limit'  // 限流：等待后重试
  }
  if (error instanceof NetworkError) {
    return 'network'     // 网络错误：等待后重试
  }
  if (error instanceof ServerError) {
    return 'server'      // 服务器错误：等待后重试
  }
  if (error instanceof AuthenticationError) {
    return 'auth'        // 认证错误：不重试，需要修复
  }
  return 'unknown'       // 其他错误：决定是否重试
}
```

### 思考模式（Thinking）

Claude Code 支持"思考模式"——AI 可以在给出最终答案之前，先展示它的推理过程：

```typescript
// 思考模式的配置
export type ThinkingConfig = {
  provider: 'anthropic'      // 使用 Claude 的内置思考能力
  budget: number            // 思考 token 预算
}

// 在 API 请求中启用
const request = {
  model: 'claude-opus-4-6',
  thinking: {
    type: 'enabled',
    budget: 10000            // 最多思考 10000 tokens
  }
}
```

### 消息历史的累积

每轮对话结束后，消息会被累积到 `mutableMessages` 中：

```typescript
// 消息类型
type Message =
  | UserMessage        // 用户消息
  | AssistantMessage   // AI 助手消息
  | SystemMessage      // 系统消息
  | ToolUseMessage     // 工具调用消息
  | ToolResultMessage  // 工具执行结果
  | ProgressMessage    // 进度消息
```

这些累积的消息形成完整的对话上下文，让 AI 能够"记住"之前的交互。

### Token 计数与成本追踪

Claude Code 会追踪每次 API 调用的 token 消耗：

```typescript
// src/cost-tracker.ts
export function getTotalCost(): number {
  return getModelUsage() * PRICE_PER_TOKEN
}

export function getTotalAPIDuration(): number {
  // 追踪 API 调用耗时
}
```

这让用户可以通过 `/cost` 命令查看当前的消耗情况。

---

## 3.2 桥接系统：IDE 是如何与 CLI 对话的？

### 为什么需要桥接系统？

Claude Code 不仅可以在终端中使用，还可以通过 VS Code 或 JetBrains 的插件来运行。桥接系统就是让 IDE 插件与 CLI 核心能够通信的桥梁。

### 整体架构

桥接系统的架构可以用下面的图来描述：

![ide-cli-bridge](./assets/ide-cli-bridge.png)

```
┌─────────────────┐         ┌─────────────────┐
│   VS Code 插件   │  WebSocket   │   CLI 核心进程   │
│  (TypeScript)   │◄──────────►│   (Bun/Bash)    │
└─────────────────┘            └─────────────────┘
        │                              │
        │  消息协议                      │  执行工具
        ▼                              ▼
┌─────────────────┐            ┌─────────────────┐
│   UI 渲染层      │            │   工具执行层     │
│ (React/Ink)     │            │ (Bash/Grep/...) │
└─────────────────┘            └─────────────────┘
```

### 核心文件结构

桥接系统的代码在 `src/bridge/` 目录下：

| 文件 | 职责 |
|------|------|
| `bridgeMain.ts` | 桥接的主循环，处理重连、心跳等 |
| `bridgeMessaging.ts` | 消息的序列化与反序列化 |
| `bridgeApi.ts` | 与远程 API 的通信 |
| `jwtUtils.ts` | JWT 认证令牌管理 |
| `sessionRunner.ts` | 会话的创建与销毁 |
| `replBridge.ts` | REPL 会话的桥接 |

### 消息协议

IDE 与 CLI 之间通过 JSON 消息进行通信：

```typescript
// 消息类型定义
type BridgeMessage =
  | { type: 'tool_call', tool: string, args: object }
  | { type: 'tool_result', tool: string, result: object }
  | { type: 'permission_request', tool: string }
  | { type: 'permission_response', allowed: boolean }
  | { type: 'status', message: string }
```

### 会话的生命周期

桥接会话有完整的生命周期管理：

```typescript
// 会话创建
async function createSession(): Promise<SessionHandle> {
  // 1. 验证客户端身份
  const token = await validateClientToken()

  // 2. 创建会话
  const session = await bridgeApi.createSession({
    clientId: token.clientId,
    sessionType: 'interactive'
  })

  // 3. 启动会话循环
  startSessionLoop(session)

  return session
}

// 会话结束
async function endSession(session: SessionHandle): Promise<void> {
  // 1. 通知对方
  await sendMessage(session, { type: 'end' })

  // 2. 清理资源
  await cleanupSession(session)

  // 3. 关闭连接
  await closeConnection(session)
}
```

### 重连与错误恢复

网络不稳定是常态，桥接系统有完善的重连机制：

```typescript
// 重连配置
const DEFAULT_BACKOFF: BackoffConfig = {
  connInitialMs: 2_000,     // 初始重连间隔 2 秒
  connCapMs: 120_000,       // 最大重连间隔 2 分钟
  connGiveUpMs: 600_000,    // 10 分钟后放弃
  generalInitialMs: 500,
  generalCapMs: 30_000,
  generalGiveUpMs: 600_000,
}

// 重连逻辑
async function withRetry<T>(operation: () => Promise<T>): Promise<T> {
  let attempt = 0
  while (true) {
    try {
      return await operation()
    } catch (error) {
      if (isRetryable(error) && attempt < maxAttempts) {
        attempt++
        await sleep(calculateBackoff(attempt))
        continue
      }
      throw error
    }
  }
}
```

### JWT 认证

桥接系统使用 JWT（JSON Web Token）进行身份验证：

```typescript
// JWT 令牌的刷新
async function refreshToken(): Promise<void> {
  const newToken = await bridgeApi.refreshToken({
    refreshToken: currentRefreshToken
  })
  scheduleTokenRefresh(newToken.expiresIn)
}

// 定时刷新
function scheduleTokenRefresh(expiresIn: number): void {
  // 在令牌过期前一段时间就刷新
  const refreshTime = expiresIn - 5 * 60 * 1000  // 提前 5 分钟
  setTimeout(refreshToken, refreshTime)
}
```

---

## 3.3 服务层：API、MCP、OAuth 都负责什么？

### 服务层概述

Claude Code 的服务层负责与外部世界的所有通信。在 `src/services/` 目录下，你可以找到各种服务的实现：

![service-layer](./assets/service-layer.png)

```
src/services/
├── api/                    # Anthropic API 调用
│   ├── claude.ts         # 核心 API 客户端
│   ├── bootstrap.ts       # 启动数据获取
│   └── errors.ts         # 错误处理
├── mcp/                   # Model Context Protocol
│   ├── client.ts         # MCP 客户端
│   ├── server.ts         # MCP 服务器
│   └── types.ts          # 类型定义
├── oauth/                 # OAuth 2.0 认证
├── analytics/             # 分析和埋点
├── compact/               # 上下文压缩
├── lsp/                   # Language Server Protocol
└── ...
```

### Anthropic API 服务

`claude.ts` 是整个系统最核心的服务之一——它负责与 Claude 的 API 通信：

```typescript
// API 调用的核心函数
async function createMessage(
  params: BetaMessageStreamParams
): Promise<Stream<BetaRawMessageStreamEvent>> {
  const apiKey = await getAPIKey()
  const baseURL = getBaseURL()

  const response = await fetch(`${baseURL}/v1/messages`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${apiKey}`,
      'Content-Type': 'application/json',
      'anthropic-version': '2023-06-01'
    },
    body: JSON.stringify(params)
  })

  return response.body
}
```

### API 请求的构建

每次发送 API 请求前，都会经过精心的构建过程：

```typescript
// 构建 API 请求的简化流程
async function buildAPIRequest(): Promise<BetaMessageStreamParams> {
  // 1. 收集系统提示词
  const systemPrompt = await fetchSystemPromptParts()

  // 2. 收集用户上下文
  const userContext = await getUserContext()

  // 3. 收集 MCP 工具
  const mcpTools = await fetchToolsForMCP()

  // 4. 构建最终请求
  return {
    model: getMainLoopModel(),
    system: combineSystemPrompts(systemPrompt, userContext),
    messages: normalizeMessagesForAPI(mutableMessages),
    tools: [
      ...coreTools,
      ...mcpTools
    ],
    max_tokens: getModelMaxOutputTokens(),
    // ...
  }
}
```

### Model Context Protocol (MCP)

MCP 是一种让 AI 与外部工具和服务集成的标准协议。你可以把它想象成"AI 的 USB 接口"——无论是什么设备，只要符合协议就能连接。

```typescript
// MCP 服务器配置示例
const mcpConfig = {
  mcpServers: {
    filesystem: {
      command: 'npx',
      args: ['@modelcontextprotocol/server-filesystem', '~/projects']
    },
    github: {
      command: 'npx',
      args: ['@modelcontextprotocol/server-github'],
      env: {
        GITHUB_TOKEN: process.env.GITHUB_TOKEN
      }
    }
  }
}
```

### MCP 工具的获取

当 MCP 服务器连接后，它们的工具会自动被发现并注册：

```typescript
// src/services/mcp/client.ts
async function connectToServer(config: McpServerConfig): Promise<MCPServerConnection> {
  // 1. 启动 MCP 进程
  const process = spawn(config.command, config.args)

  // 2. 初始化握手
  const { tools, resources } = await initializeProtocol(process)

  // 3. 注册工具
  for (const tool of tools) {
    registerTool({
      name: tool.name,
      description: tool.description,
      inputSchema: tool.inputSchema,
      handler: (input) => callMCPProcess(process, tool.name, input)
    })
  }

  return { process, tools, resources }
}
```

### OAuth 认证

OAuth 服务负责处理第三方认证（比如 GitHub 登录）：

```typescript
// OAuth 流程
async function startOAuthFlow(provider: 'github' | 'google'): Promise<string> {
  // 1. 生成随机 state（用于 CSRF 防护）
  const state = randomUUID()

  // 2. 跳转到授权页面
  const authUrl = buildOAuthURL(provider, { state })

  // 3. 等待回调
  const callback = await waitForOAuthCallback(provider, state)

  // 4. 交换令牌
  const tokens = await exchangeCodeForTokens(callback.code)

  // 5. 保存令牌
  await saveTokens(tokens)

  return tokens.accessToken
}
```

### 分析服务

分析服务负责收集使用数据（当然是在用户同意的情况下）：

```typescript
// 使用事件记录
function logEvent(
  event: string,
  metadata: Record<string, unknown>
): void {
  if (isAnalyticsDisabled()) return

  queue.push({
    event,
    timestamp: Date.now(),
    metadata
  })

  // 批量发送
  if (queue.length >= BATCH_SIZE) {
    flushQueue()
  }
}
```

---

## 3.4 启动优化：并行预加载让启动更快

### 启动速度的重要性

对于一个 CLI 工具来说，启动速度至关重要。没有人愿意等好几秒才能开始工作。Claude Code 的 main.tsx 只有约 80 万字节，但加载的模块却非常复杂。

### 预加载策略

Claude Code 在真正开始加载代码之前，就已经开始准备数据了：

```typescript
// main.tsx 的开头
// 这些副作用必须在所有其他导入之前运行！

// 1. 启动性能分析
profileCheckpoint('main_tsx_entry')

// 2. 并行启动 MDM 读取（企业配置）
// MDM (Mobile Device Management) 是 macOS 的配置系统
startMdmRawRead()

// 3. 并行启动 Keychain 预读取
// 这对于认证至关重要
startKeychainPrefetch()

// 4. 预连接 API
prefetchAPIConnection()
```

### 关键路径分析

启动过程可以分为"关键路径"（必须完成的）和"非关键路径"（可以延迟的）：

![startup-timeline](./assets/startup-timeline.png)

```
┌─────────────────────────────────────────────────────────────┐
│                         启动时间线                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  0ms ───────┬────────────────────────────────────►         │
│             │                                              │
│  ├─ MDM 读取 (并行)                                        │
│  ├─ Keychain 预读取 (并行)                                   │
│  │                                                          │
│  50ms ──────┼────────────────────────────────────►         │
│             │                                              │
│  ├─ 加载核心模块 (必须等待)                                   │
│  │                                                          │
│  100ms ─────┼────────────────────────────────────►         │
│             │                                              │
│  ├─ 加载插件 (可以延迟)                                      │
│  ├─ 初始化分析服务 (可以延迟)                                 │
│  │                                                          │
│  200ms ─────┼────────────────────────────────────►         │
│             │                                              │
│  └─ 启动完成，准备接受输入                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 延迟加载重型模块

一些重型模块会延迟到真正需要时才加载：

```typescript
// React/Ink 组件只在需要 UI 时加载
/* eslint-disable @typescript-eslint/no-require-imports */
const MessageSelector =
  (): typeof import('src/components/MessageSelector.js') =>
    require('src/components/MessageSelector.js')
/* eslint-enable @typescript-eslint/no-require-imports */

// OpenTelemetry 等重型库延迟加载
const openTelemetry = await import('opentelemetry')
```

### 并行初始化

多个初始化任务并行执行，减少总等待时间：

```typescript
// 并行执行多个初始化任务
await Promise.all([
  fetchBootstrapData(),          // 获取启动数据
  loadRemoteManagedSettings(),   // 加载托管配置
  refreshPolicyLimits(),         // 刷新策略限制
  initializeGrowthBook(),        // 初始化分析
])
```

### 缓存与复用

启动时会尝试复用已有的缓存：

```typescript
// 缓存的设置
const cachedConfig = getGlobalConfig()  // 优先从缓存读取

// 缓存的 MCP 配置
const cachedMCPConfig = getCachedMCPConfig()

// 缓存的插件列表
const cachedPlugins = loadAllPluginsCacheOnly()
```

---

## 3.5 条件编译：只编译需要的功能（feature flags）

### 什么是 feature flags？

Feature flags（功能开关）是一种强大的技术，它允许你在不发布不同版本的情况下，动态启用或禁用功能。

Claude Code 使用 Bun 内置的 `feature()` 函数来实现这个功能：

```typescript
import { feature } from 'bun:bundle'

// 如果 VOICE_MODE 功能未启用，这段代码就不会被包含在最终产物中
if (feature('VOICE_MODE')) {
  const voiceModule = require('./commands/voice/index.js')
  // ...
}
```

### 工作原理

Bun 的 `feature()` 函数在构建时会根据环境变量来决定包含哪些代码：

```bash
# 构建时指定启用的功能
bun build --target node --feature VOICE_MODE --feature KAIROS src/main.tsx
```

这就像乐高套装中的"可选包"——你只拿到你购买的那些部件。

### Feature 列表

Claude Code 定义了众多的 feature flags，用于控制各种功能的启用与禁用。以下是一些主要的 feature flags：

![feature-flags](./assets/feature-flags.png)

| Feature | 说明 |
|---------|------|
| `PROACTIVE` | 主动模式 |
| `KAIROS` | KAIROS 助手模式 |
| `BRIDGE_MODE` | IDE 桥接模式 |
| `DAEMON` | 守护进程模式 |
| `VOICE_MODE` | 语音模式 |
| `AGENT_TRIGGERS` | 定时代理触发 |
| `MONITOR_TOOL` | 监控工具 |
| `COORDINATOR_MODE` | 协调器模式 |
| `HISTORY_SNIP` | 历史截断 |
| `EXPERIMENTAL_SKILL_SEARCH` | 实验性技能搜索 |
| `BUDDY` | Companion Sprite 伴侣模式 |
| `CHICAGO_MCP` | 计算机控制模式 |
| `BG_SESSIONS` | 后台会话管理 |
| `CONTEXT_COLLAPSE` | 上下文压缩 |
| `TOKEN_BUDGET` | Token 预算控制 |
| `CACHED_MICROCOMPACT` | 缓存式微压缩 |
| `REACTIVE_COMPACT` | 响应式压缩 |
| `WORKFLOW_SCRIPTS` | 工作流脚本 |
| `BYOC_ENVIRONMENT_RUNNER` | 自带环境运行器 |
| `SELF_HOSTED_RUNNER` | 自托管运行器 |
| `TEAMMEM` | 团队记忆同步 |
| `REVIEW_ARTIFACT` | 审查产物支持 |
| `RUN_SKILL_GENERATOR` | 技能生成器 |
| `BUILDING_CLAUDE_APPS` | Claude Apps 构建支持 |

### 代码示例

Feature flags 的使用非常普遍：

```typescript
// src/commands.ts
const proactive =
  feature('PROACTIVE') || feature('KAIROS')
    ? require('./commands/proactive.js').default
    : null

const voiceCommand = feature('VOICE_MODE')
  ? require('./commands/voice/index.js').default
    : null

const bridge = feature('BRIDGE_MODE')
  ? require('./commands/bridge/index.js').default
    : null
```

### 条件编译的优势

这种技术带来了几个显著的优势：

1. **减小包体积**：不需要的功能不会被打包
2. **灰度发布**：可以逐步启用新功能
3. **A/B 测试**：可以同时测试不同版本的实现
4. **环境适配**：不同版本（内部版、企业版）可以有不同功能

### 与 TypeScript 的配合

虽然这不是严格的条件编译，但 TypeScript 的类型系统也为代码的条件化提供了支持：

```typescript
// 类型层面
type FeatureFlags = {
  VOICE_MODE: boolean
  KAIROS: boolean
  // ...
}

// 类型化的特性检查
function checkFeature(flag: keyof FeatureFlags): boolean {
  return feature(flag)
}
```

---

## 3.6 代码规模与目录结构再探

### 从数字看规模

让我们再回顾一下 Claude Code 的代码规模：

| 指标 | 数值 |
|------|------|
| 文件总数 | ~1,900 |
| 代码行数 | ~512,000 |
| TypeScript 文件 | ~1,800 |
| 工具定义 | ~40 种 |
| Slash 命令 | ~50 个 |
| React 组件 | ~140 个 |

### 核心文件的作用

让我们更详细地看看几个关键文件的职责：

```
src/
├── main.tsx (~80万字节)
│   └── 应用入口，并行初始化，性能分析
│
├── QueryEngine.ts (~4.6万字节)
│   └── 核心 AI 查询循环，消息处理，工具调用
│
├── Tool.ts (~3万字节)
│   └── 工具的基础类型和接口定义
│
├── commands.ts (~2.5万字节)
│   └── 所有 slash 命令的注册表
│
├── tools.ts
│   └── 所有工具的注册表
│
├── context.ts
│   └── 用户上下文和系统上下文的收集
│
└── cost-tracker.ts
    └── Token 消耗追踪
```

### 目录组织的哲学

Claude Code 的目录组织遵循了几个原则：

1. **按功能分**：所有 API 相关代码在 `services/api/`
2. **按领域分**：所有 MCP 相关代码在 `services/mcp/`
3. **按层次分**：`commands/` 是命令，`tools/` 是工具，`components/` 是 UI

### 模块间的依赖管理

面对如此规模的代码，模块间的依赖管理至关重要：

```typescript
// 使用命名导入避免循环依赖
import { feature } from 'bun:bundle'

// 使用延迟导入避免循环依赖
/* eslint-disable @typescript-eslint/no-require-imports */
const getTeammateUtils = () =>
  require('./utils/teammate.js')
/* eslint-enable @typescript-eslint/no-require-imports */

// 使用类型注入而不是直接导入
type ToolUseContext = DeepImmutable<{
  options: ToolOptions
  // ...
}>
```

### 死代码消除

除了 feature flags，代码中还使用了一些其他技术来消除死代码：

```typescript
// 使用 lodash-es 的 Tree Shaking 优化
import uniqBy from 'lodash-es/uniqBy.js'

// 只导入使用的部分
import { cloneDeep } from 'lodash-es/cloneDeep.js'
```

---

## 本章小结

在这一章中，我们深入到了 Claude Code 的实现细节：

| 主题 | 核心要点 |
|------|----------|
| **QueryEngine** | AI 调用的核心，处理消息循环、工具调用、错误重试、思考模式 |
| **桥接系统** | IDE 与 CLI 通信的桥梁，使用 WebSocket + JSON 协议，有完善的重新连接机制 |
| **服务层** | 分为 API、MCP、OAuth、分析等多个服务，各司其职 |
| **启动优化** | 并行预加载关键资源、延迟加载重型模块、缓存复用 |
| **条件编译** | 通过 `feature()` 实现"一码多构"，减小包体积，支持灰度发布 |

这些技术的组合，让 Claude Code 能够在保持功能丰富的同时，保持良好的性能和启动速度。

---

## 延伸阅读

如果你想进一步了解这些系统，建议阅读以下源文件：

- `src/QueryEngine.ts` — 查询引擎的核心实现
- `src/bridge/bridgeMain.ts` — 桥接系统的主循环
- `src/services/api/claude.ts` — Anthropic API 的调用封装
- `src/main.tsx` — 启动流程和并行初始化
- `src/constants/prompts.ts` — Feature flags 的实际使用示例

---

[下一章：Prompt 的艺术——如何让 AI 正确做事？](./04-Prompt的艺术：如何让AI正确做事.md)